{
  "name": "diceroll",
  "description": "Roll any dice combination, in the browser and on Android",
  "license": "MIT OR Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "sync": "cap sync android",
    "test": "node --test \"tests/**/*.test.js\""
  },
  "dependencies": {
    "@capacitor/android": "^8.5.0",
    "@capacitor/core": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
